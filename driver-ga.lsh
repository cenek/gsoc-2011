(libload "libc/make")
(libload "setup.lsh")
(libload "problem1.lsh")
(libload "ga.lsh")


(defun test ()
  (let* ((pr (new Problem))
	 (ga (new GeneticAlgorithm pr)))

    (==> ga evolution 10 10 10 5 0 "")
    ))
